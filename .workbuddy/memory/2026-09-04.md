# 2026-09-04 工作记录

## 07:00 房地产日报（自动化 1782660080338，D-1=2026-09-03）
- 全链路成功，无异常。环境 [READY]（Chrome 9222 / CDP 3456 本来就活着，cookie warm 跳过预热）
- 抓取 `node fangdi_cdp_proxy_scraper.js --mode=secondhand --date=2026-09-03` 退出码 0
- 数据：一手 714套/62141㎡/套均87.03/可售57227（昨晚 23:50 已抓，无需兜底）；二手 637套/52879㎡/套均83.01/挂牌318783
- 楼市回顾：新开 3 个单元 188 套；预/出售 713 套 6.19 万㎡（与一手 714 套基本吻合）
- 腾讯文档：bwHrDx UPDATE rid=r1WMvs（8 字段回读全对）；GxaSD5 ADD rid=rnqUg2（TS=1788393600000，原文未改写）
- GitHub：pull --rebase --autostash fast-forward（远端仅 subscription_data.json 变动）；commit `update data 2026-09-03`(980af40) push 成功
- 微信三人（梦比鱿鱼丝/沈胄磊/周凯琦）均 DONE；哨兵 `data/.sent_2026-09-03` 已写且确认被 .gitignore 忽略
- 终检：fangdi_data.json 顶部日期 09-03，09-03 唯一 1 条，**未误建 09-04 记录** ✅；git 工作区 data/ 已干净
- 抓取改用 `> data/_scrape_xxx.log 2>&1` 重定向再读（避免 `| head` 的 SIGPIPE 让 node 退出码误报 1），用完即删

## 08:30 认购公示同步失败排查与根治（主人反馈触发）
- 现象：07:15 认购任务报告「数据更新成功 + GitHub 已推送，但腾讯文档同步失败 no_token」，列出 3 个新楼盘
- 核查结论：**GitHub 其实推送成功**（commit `b1df917 更新认购公示数据至2026-09-04`，78 条）；
  失败的只有腾讯文档一步 —— 该步骤走了已失效旧通道（`tencentdocs.py tdoc_init`）。
  对比佐证：07:00 日报任务每天都能通过 `tdoc_mcp.py` 成功同步腾讯文档 → 自动化环境通道本身没问题
- 人工补录 3 条（10/10 字段完整，回读校验通过，表 89→92 条）：
  悦海棠苑二期(青浦)281套62814→`rcyP0a`；天誉兰园(闵行)132套74714→`rpuzXL`；水岸和煦名邸(闵行)96套68498→`rVPR7O`
- **根治①**：`scrape_subscription_data.js` 内置 AUTO-SYNC —— 发现新增楼盘即自动调
  `Python311 sync_subscription_0g5JQL.py --date <今天>`（`--no-sync` 可关），不再依赖调用方记得同步；
  失败只告警不改退出码。端到端实测：造一个"新增"→自动触发同步→去重报 NO_CHANGE，幂等 ✅
- **根治②**：`tdoc_helper.py` 改为垫片（优先 tdoc_mcp，mcporter 仅兜底），call/ok 签名不变，旧脚本零改动恢复可用，实测探测 OK
- git：pull --rebase --autostash 触发 updateTime 冲突（我 08:33 重跑抓取改了时间戳，与远端 07:15 版本撞车）；
  双方 78 条内容完全一致 → 取远端版本 + `git stash drop`，未丢数据
- 顺手把 `tdoc_mcp.py`/`tdoc_helper.py`/`sync_subscription_0g5JQL.py` 纳入版本库（此前一直 untracked）

## 13:0x 看板认购区看不到新增楼盘（主人提问）
- 现象：3 个新楼盘数据已在腾讯文档和 GitHub，但看板认购表不显示
- 排查：**数据没问题** —— Pages 部署的是仓库根目录，`/data/subscription_data.json` 线上 200 且已含 3 个新楼盘
- 根因：前端过滤 `startDate <= today` 只显示"已开始"的；3 个新盘 9/5 才开始认购（明天）→ 被过滤。
  讽刺的是代码里本就有 `if (today < startDate) status='即将开始'` 分支，但过滤条件让该分支**永不触发**
- 修复：过滤窗口改为 **过去10天 ~ 未来14天**（认购公示本质是"预告"，楼盘常提前1~3天公示）。
  三份都改：根目录 `index.html`(Pages实际用) + `docs/index.html` + `dashboard/index.html`
- 本地模拟验证：旧逻辑命中 5 条（溯阳云筑等），新逻辑命中 8 条（3 个新盘排最前）
- commit `390d8fd` push 成功；75s 后 curl 线上确认已部署生效

## 13:3x 逐项核验"都能生效"+ 建立工作区整洁约定（主人要求）
- **核验结果：全部生效** ✅
  - 线上看板：`curl index.html` 命中 `fourteenDaysLater` ×3；线上 `data/subscription_data.json` date=2026-09-04/78条，3 个新盘（9/5 认购）均在
  - 腾讯文档 0g5JQL：92 条，数据日期=2026-09-04 命中 3 条（rcyP0a/rpuzXL/rVPR7O），字段完整
  - 脚本层：AUTO-SYNC 已焊入 scrape_subscription_data.js（--no-sync 可关）；tdoc_helper.py 垫片优先走 tdoc_mcp ✅
  - 入库自查：tdoc_mcp/tdoc_helper/sync_subscription/scrape_subscription 已 tracked；发现 **work_log_sync.py 仍 UNTRACKED**（核心链路，已补入库）
- **"不要存不想干的"落地**：.gitignore 新增"临时产物"段（根目录 `/*.png`、`/*_2026-*.py|js`、`/test_*.js`、
  `/transform_for_dashboard_v2.js`；data 下 `.notify_*/.alert_*/.watchdog_*/wechat_*.txt/land_wechat_*/work_log_*.md/_scrape_*.log`）
  → 未跟踪文件 **177 → 114**
- 补入库 11 个在用核心脚本（work_log_sync / sync_07am_bwHrDx / sync_07am_GxaSD5 / sync_2350_bwHrDx / land_tdoc_sync /
  gen_land_xlsx / fix_incomplete_0g5JQL / backfill_plan.json / send_worklog_reminder / send_lpr_202608 / dedup_tdoc）+ .gitignore，commit `4847d79` 已 push
- 遗留待主人确认清理（**未擅自删**）：`old_scripts/` 58 个归档旧脚本；根目录废弃脚本（explore_fangdi_page.js、
  fetch_subscription_data_v2/v3.js、probe_tdoc.py、*.backup、screenshot.py/ps1、screenshot_desktop.py）；
  过期文件 security_audit_2026-08-16.md、wechat_alert_2026-08-18.txt；`fangdi-monitor/`(仅 config.json+output)；
  待定 sync_07am_task.py / sync_23pm_newhouse.py（疑为早期版本，未入库）

## 13:4x 清理执行（主人: "冷删除"）
- 方式：**回收站删除**（可还原）。PowerShell `Add-Type` 被安全策略拦（`Add-Type compiles and loads .NET code at runtime`）→
  改用 Python311 `win32com.shell.shell.SHFileOperation((0, FO_DELETE, path, None, FOF_ALLOWUNDO|FOF_NOCONFIRMATION|FOF_SILENT, None, None))`，rc=0 全部成功
- 分三批执行 + 逐批验证：
  ① `old_scripts/` 58 文件 → GONE_OK
  ② 根目录废弃脚本 9 个：explore_fangdi_page.js / fetch_subscription_data_v2.js / v3.js / probe_tdoc.py /
     fangdi_cdp_scraper.js.backup / parse_detail_optimized.py.backup / screenshot.py / screenshot.ps1 / screenshot_desktop.py
  ③ security_audit_2026-08-16.md / wechat_alert_2026-08-18.txt / fangdi-monitor/(config.json+空output)
  ④ 追加清理早期版 sync_07am_task.py（硬编码 DATE='2026-08-18'）、sync_23pm_newhouse.py（已被 sync_2350_bwHrDx.py 取代）
- 保留并**补入库**：crop_wb.py / crop_wechat.py / screenshot_hwnd.py（主人在用的截屏工具，commit `19cb92b`）
- 结果：工作区未跟踪文件 **177 → 0**（仅剩 .workbuddy/ 记忆，必须留），git 工作区干净
- 清理后回归验证（防删到依赖）：12 个核心 py 全部 `py_compile` OK；4 个核心 js 全部 `node --check` OK；
  tdoc_mcp 通道连通 OK；grep 确认无脚本残留引用已删模块

## 17:30 工作日志提醒（自动化 1782772611455）
- 微信 + 对话双通道发出，微信 returncode=0 / DONE
- 新规落地：提醒文本临时落 `data/wechat_worklog_reminder_20260904.txt`，**发完即删**（回收站），不再留堆积
- 复检：工作区未跟踪文件仍为 0（仅剩 .workbuddy 记忆）
